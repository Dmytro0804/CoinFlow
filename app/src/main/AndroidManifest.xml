<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:name=".CoinFlowApp"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CoinFlow">

        <receiver android:name=".Helper.PlanExpirationReceiver" android:exported="false" />

        <activity
            android:name=".Activity.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".Activity.MainActivity"
            android:exported="false" />

        <activity android:name=".Activity.RegisterActivity" />
        <activity android:name=".Activity.DetailCryptoActivity" />
        <activity android:name=".Activity.DepositActivity" />
        <activity android:name=".Activity.ProfileActivity" />
        <activity android:name=".Activity.WithdrawActivity" />
        <activity android:name=".Activity.ConvertActivity" />
        <activity android:name=".Activity.GraphActivity" />
        <activity android:name=".Activity.AccountsActivity" />
        <activity android:name=".Activity.StatisticsActivity" />
        <activity android:name=".Activity.NoInternetActivity" />
        <activity android:name=".Activity.PlanActivity" />
        <activity android:name=".Activity.AssistantActivity" />

    </application>

</manifest>